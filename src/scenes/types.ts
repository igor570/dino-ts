//my custom types! :)
export type Trigger = Phaser.Types.Physics.Arcade.SpriteWithDynamicBody;
export type Terrain = Phaser.GameObjects.TileSprite;

export interface Canvas {
  height: number;
  width: number;
}
